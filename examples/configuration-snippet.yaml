# Optional: YAML-Setup ohne UI-Konfiguration.
# Für die HACS-Installation ist normalerweise kein YAML nötig. Nach der
# Installation einfach unter Einstellungen -> Geräte & Dienste die Integration
# "Essensplaner" hinzufügen.

essen:

sensor:
  - platform: essen
